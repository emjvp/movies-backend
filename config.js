const config = {
    db: {
      /* don't expose password or any sensitive info, done only for demo */
      host: "localhost",
      user: "emjvp",
      password: "emjvp",
      database: "movies",
    },
    // listPerPage: 10,
  };
  module.exports = config;